@model IEnumerable<travel_agency.Models.Topicality>

@{
    ViewBag.Title = "Index";
}

<div class=" jumbotron w3-container w3-center w3-animate-bottom">
    <h1>Aktualności</h1>
</div>
@if (Context.User.IsInRole("Admin"))
 {
    <p>
        @Html.ActionLink("Dodaj nową aktualność", "Create")
    </p>
 }

<span onclick="ustawslajd(1)" style="cursor:pointer;">[ 1 ]</span>
<span onclick="ustawslajd(2)" style="cursor:pointer;">[ 2 ]</span>
<span onclick="ustawslajd(3)" style="cursor:pointer;">[ 3 ]</span>
<span onclick="ustawslajd(4)" style="cursor:pointer;">[ 4 ]</span>
<span onclick="ustawslajd(5)" style="cursor:pointer;">[ 5 ]</span>

<div id="slider"></div>
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<div class="w3-container w3-center w3-animate-bottom">


    @foreach (var item in Model)
    {
    <div class="cont">
        <p>
            <h3> @Html.DisplayFor(modelItem => item.TripDescription)</h3>
        </p>
        @if (Context.User.IsInRole("Admin"))
        {
    <p>
        @Html.ActionLink("Usuń | ", "Delete", new { id = item.ID }, new { @class = "btn btn-primary btn-large button2" })
        @Html.ActionLink("Szczegóły | ", "Details", new { id = item.ID }, new { @class = "btn btn-primary btn-large button3" })
        @Html.ActionLink("Edycja", "Edit", new { id = item.ID }, new { @class = "btn btn-primary btn-large button1" })
    </p>
        }


    </div>
        <br />
        <br />

    }


</div>
